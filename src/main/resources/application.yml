server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/NESTRO
    username: postgres
    password: postgres
  mvc:
    hiddenmethod:
      filter:
        enabled: true



#liquibase:
#  change-log: classpath:db/changelog/db.changelog-master.yaml
#    enabled: true